// ============================================================
// INTRO / ENVELOPE
// Opening screen shown before the invitation is entered.
// ============================================================

export default function Intro({ children }: { children: React.ReactNode }) {
  return <div data-component="intro">{children}</div>;
}
