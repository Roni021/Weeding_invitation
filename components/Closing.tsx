// ============================================================
// CLOSING SECTION
// Purpose: Closing message
// ============================================================

export default function Closing({ children }: { children: React.ReactNode }) {
  return (
    <section id="closing" data-section="closing">
      {children}
    </section>
  );
}
