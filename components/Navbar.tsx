// ============================================================
// NAVIGATION
// Desktop navigation, mobile menu trigger and scroll state.
// ============================================================

export default function Navbar({ children }: { children: React.ReactNode }) {
  return <div data-component="navbar">{children}</div>;
}
