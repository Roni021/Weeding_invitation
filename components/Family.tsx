// ============================================================
// FAMILY SECTION
// Purpose: Family blessings
// ============================================================

export default function Family({ children }: { children: React.ReactNode }) {
  return (
    <section id="family" data-section="family">
      {children}
    </section>
  );
}
