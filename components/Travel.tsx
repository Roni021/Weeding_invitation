// ============================================================
// TRAVEL SECTION
// Purpose: Travel information
// ============================================================

export default function Travel({ children }: { children: React.ReactNode }) {
  return (
    <section id="travel" data-section="travel">
      {children}
    </section>
  );
}
