// ============================================================
// CONTACT SECTION
// Purpose: Family contacts
// ============================================================

export default function Contact({ children }: { children: React.ReactNode }) {
  return (
    <section id="contact" data-section="contact">
      {children}
    </section>
  );
}
