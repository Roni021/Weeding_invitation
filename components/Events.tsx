// ============================================================
// WEDDING EVENTS SECTION
// Purpose: Wedding functions
// ============================================================

export default function Events({ children }: { children: React.ReactNode }) {
  return (
    <section id="events" data-section="events">
      {children}
    </section>
  );
}
