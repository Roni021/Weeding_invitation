// ============================================================
// SAVE THE DATE SECTION
// Purpose: Calendar tools
// ============================================================

export default function SaveTheDate({ children }: { children: React.ReactNode }) {
  return (
    <section id="savedate" data-section="savedate">
      {children}
    </section>
  );
}
