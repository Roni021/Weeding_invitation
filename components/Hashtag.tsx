// ============================================================
// HASHTAG / SOCIAL SECTION
// Purpose: Social sharing
// ============================================================

export default function Hashtag({ children }: { children: React.ReactNode }) {
  return (
    <section id="hashtag" data-section="hashtag">
      {children}
    </section>
  );
}
