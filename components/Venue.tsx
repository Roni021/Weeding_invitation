// ============================================================
// VENUE SECTION
// Purpose: Venue and map
// ============================================================

export default function Venue({ children }: { children: React.ReactNode }) {
  return (
    <section id="venue" data-section="venue">
      {children}
    </section>
  );
}
