// ============================================================
// COUPLE SECTION
// Purpose: Bride and groom
// ============================================================

export default function Couple({ children }: { children: React.ReactNode }) {
  return (
    <section id="couple" data-section="couple">
      {children}
    </section>
  );
}
